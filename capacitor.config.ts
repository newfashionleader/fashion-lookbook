import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fashionweek.app',
  appName: 'fashion-lookbook',
  webDir: 'dist/fashion-lookbook/browser'
};

export default config;
